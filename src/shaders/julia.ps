Texture2D tex : register(t0);
SamplerState linear_sampler : register(s0);

struct ps_in
{
	float4 pos : SV_Position;
	float2 texcoord : TEXCOORD;
};

float4
main(ps_in input) : SV_Target
{
	return (tex.Sample(linear_sampler, input.texcoord));
}

